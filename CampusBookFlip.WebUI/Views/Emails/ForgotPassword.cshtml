@model CampusBookFlip.WebUI.Models.ForgotPasswordEmail

@{
    Layout = null;
}

To: @Model.To
From: @Model.From
Subject: @Model.Subject

Hello @Model.FirstName,

Don't you hate it when that happens? Don't worry, just visit @(new System.UriBuilder(Request.Url.AbsoluteUri) { Path = Url.Action("ResetPassword", "Account", new { token = Model.ConfirmationToken, secret = Model.SharedSecret }) }.ToString()) before @Model.Expiration and you'll be back to flipping books in no time!

- Campus BookFlip Team
